# sample (broken)

Invariant 1 violation: references `${CLAUDE_PLUGIN_ROOT}/knowledge/missing.md`
which does not exist.

Invariant 2 violation: escapes its plugin via `${CLAUDE_PLUGIN_ROOT}/../shared/x.md`.
